{
  "name": "createvideolinkstreamtape",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "postinstall": "PUPPETEER_CACHE_DIR=./.cache/puppeteer npx puppeteer browsers install chrome",  
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "PUPPETEER_CACHE_DIR=./.cache/puppeteer node server.js",
    "build": "npm install puppeteer express && npx puppeteer install",
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
